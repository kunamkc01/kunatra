# Copy to .env and fill in. Without DATABASE_URL the API serves the bundled sample.
PORT=4100
DATABASE_URL=postgres://localhost:5432/kunatra
# Application-layer key used to encrypt Aadhaar/PAN before they reach the DB.
# Never commit the real value.
FIELD_ENCRYPTION_KEY=change-me
